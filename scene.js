const arr=[1,2,3]
const arr1=(arr.map(n=>1+n))
console.log('arr1',arr1)
const f=(()=>a.fft[2])
voronoi(1,1,1).color(f).modulate(o0,Math.PI).out(o0)
shape(arr1).modulatePixelate(osc(1,1,1)).modulate(o1,0.5).add(o0).out(o1)
render(o1)
